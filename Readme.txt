You can execute the program by running the Controller.java class (situated under root package), which contains the main method.
To enable attacks and their solutions you can change the parameters of the object created in the class to true.  
